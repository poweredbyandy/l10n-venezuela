Open a record of the report's model and use **Print > <report name>**. A dialog shows the PDF rendering of the exact ESC/P stream; click **Imprimir** to send it to the Epson printer selected in the browser USB dialog.

Business modules may also open the preview from their own buttons (for example the invoice *Print* button when the journal uses continuous paper).
