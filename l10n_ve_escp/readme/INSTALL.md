Install the ESC/P interpreter used to render the preview PDF (PyPI name differs from the import name):

```bash
pip install pyscape
```

Without it the module still prints; the preview falls back to a styled character grid.

WebUSB printing requires Google Chrome or Microsoft Edge and an HTTPS (or localhost) backend. Grant USB access to the Epson printer when the browser asks for it.
